# Alternative Smithery configuration - try this if main config fails
startCommand:
  type: stdio
configSchema:
  type: object
  properties: {}
commandFunction: |-
  (config) => ({
    command: 'python',
    args: ['-u', 'server.py']
  })
exampleConfig: {}
